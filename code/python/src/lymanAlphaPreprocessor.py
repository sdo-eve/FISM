import subprocess
# -----------------------------------------------------------------------------
# -----------------------------------------------------------------------------
class lymanAlphaPreprocessor:
    
    def preprocess(self, LOCAL_ROOT):
        print "in preprocess, dataDestRoot is " + dataDestRoot
#        subprocess.call(["get_lya.pl", "args"],shell=True)

        # cd to dir where lymanAlpha data file is staged.
        
        # Strip comments, format info, keep just the data
        # Read lya data file line by line
        # if line starts with ;, toss
        
        
        

 